export interface SearchedCity {
    name: string,
    state: string,
    country: string,
    lat: number,
    lon: number,
}

export interface Favorites {
    id: string;
    name: string,
    state: string,
    country: string,
    lat: number,
    lon: number,
}